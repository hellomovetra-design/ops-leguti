"use client";
import { useEffect, useState } from "react";
type User = { id?: string; email: string; role: string; leader_name?: string };
export function AdminRoleManager() {
  const [rows, setRows] = useState<User[]>([]), [email, setEmail] = useState(""), [role, setRole] = useState("admin"), [leader, setLeader] = useState(""), [password, setPassword] = useState(""), [message, setMessage] = useState("");
  const load = async () => { const r = await fetch("/api/ops-desk?type=users"); const x = await r.json(); setRows(x.items || []); };
  useEffect(() => { load(); }, []);
  const save = async () => { const r = await fetch("/api/ops-desk", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ action: "role", email: email.trim().toLowerCase(), role, leaderName: leader.trim(), password }) }); const x = await r.json(); setMessage(r.ok && x.ok ? "Role berhasil dibuat" : x.error || "Gagal membuat role"); if (r.ok && x.ok) { setEmail(""); setLeader(""); setPassword(""); load(); } };
  const roleCopy: Record<string, string> = { admin: "Akses operasional dashboard dan PWA sesuai divisi.", leader: "Akses monitoring dan data tim leader.", spv: "Akses luas seperti admin untuk memantau seluruh operasi dan struktur tim.", jr_spv: "Akses luas seperti admin untuk memantau operasi hub dan struktur tim.", pending: "Akun menunggu penetapan akses." };
  return <><div className="section-head"><div><div className="eyebrow">SECURITY · SUPER ADMIN</div><h1 className="headline">Administrator</h1><div className="subtitle">Buat dan kelola akses pengguna operasional secara terpusat.</div></div></div><div className="admin-role-layout"><div className="card role-create-card"><div className="role-create-head"><div><div className="card-title">Tambah administrator</div><div className="card-sub">Undang pengguna baru dan tentukan hak aksesnya.</div></div><span className="role-badge">SUPER ADMIN</span></div><div className="role-form"><label><span>Email pengguna</span><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="nama@jne.co.id"/><small>Gunakan email yang akan dipakai untuk login aplikasi.</small></label><label><span>Jenis akses</span><select value={role} onChange={e=>setRole(e.target.value)}><option value="admin">Admin lapangan</option><option value="leader">Leader</option><option value="spv">SPV</option><option value="jr_spv">Jr. SPV</option><option value="pending">Pending</option></select><small>{roleCopy[role]}</small></label><label><span>Password sementara</span><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Minimal 8 karakter"/><small>Berikan password sementara kepada pengguna untuk login pertama.</small></label><label><span>Nama leader / divisi</span><input value={leader} onChange={e=>setLeader(e.target.value)} placeholder="Contoh: Admin Inbound atau SPC LEGUTI"/><small>Opsional, untuk menandai cakupan pengguna.</small></label></div><button className="primary full" disabled={!email.trim()||password.length<8} onClick={save}>Buat role administrator</button>{message&&<div className="toast" role="status">{message}</div>}</div><div className="card role-guide"><div className="card-title">Alur akses</div><div className="role-step"><b>1</b><span><strong>Masukkan email</strong><small>Pastikan email pengguna benar.</small></span></div><div className="role-step"><b>2</b><span><strong>Pilih jenis akses</strong><small>Akses menentukan menu yang tersedia.</small></span></div><div className="role-step"><b>3</b><span><strong>Simpan role</strong><small>Pengguna dapat login menggunakan Supabase Auth.</small></span></div></div></div><div className="card role-list-card"><div className="card-head"><div><div className="card-title">Pengguna terdaftar</div><div className="card-sub">Daftar akses administrator yang sudah dibuat.</div></div><span>{rows.length} akun</span></div><div className="table-wrap"><table><thead><tr><th>Email pengguna</th><th>Jenis akses</th><th>Leader / Divisi</th></tr></thead><tbody>{rows.map(x=><tr key={x.id||x.email}><td><strong>{x.email}</strong></td><td><span className="pill">{x.role==="admin"?"Admin lapangan":x.role}</span></td><td>{x.leader_name||"—"}</td></tr>)}</tbody></table>{!rows.length&&<div className="empty"><strong>Belum ada administrator tambahan</strong><small>Gunakan form di atas untuk membuat role pertama.</small></div>}</div></div></>
}




